---
route: /element/knob
---

import { Playground } from "docz";
import styled from "styled-components";

import { Knob } from "./Knob";

## Knob

Knobs are basically sliders with weird styles.

<Playground>
  <Knob min={0} max={1} radius={72} color="#FF0000" />
  <Knob min={0} max={100} radius={72} color="#00FF00" />
  <Knob min={-100} max={100} radius={72} color="#FFFFFF" />
</Playground>
